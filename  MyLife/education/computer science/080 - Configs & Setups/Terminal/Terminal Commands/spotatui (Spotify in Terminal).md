To install spotatui, you can use:

```bash
cargo install spotatui
```

You have to install it with cargo, because brew doesn't feature this application.

To start it just use:

```bash
spotatui
```

This will start the application, after logging in, you are ready to start.

