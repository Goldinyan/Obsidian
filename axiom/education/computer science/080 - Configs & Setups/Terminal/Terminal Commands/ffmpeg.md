Convert it to PPM using tools like:

### **Using `ffmpeg` (Command Line)**

```bash
ffmpeg -i input.jpg output.ppm
```

- Replace `input.jpg` with your image file.
- This will generate a binary PPM file (`output.ppm`).